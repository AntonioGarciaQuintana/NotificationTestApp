# NotificationTestApp
Angular-based frontend application responsible for interacting with the backend notification system. It allows users to send notifications by selecting a category and providing a message, and includes a dedicated view to display the history of sent notifications.
